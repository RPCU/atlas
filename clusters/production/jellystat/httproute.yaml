apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: jellystat
spec:
  parentRefs:
    - name: https
      namespace: kgateway-system
  hostnames:
    - "jellystat.production.rpcu.lan"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: jellystat-oauth2-proxy
          port: 80
