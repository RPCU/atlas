apiVersion: apps/v1
kind: Deployment
metadata:
  name: palworld
  labels:
    app: palworld
spec:
  strategy:
    type: Recreate
  replicas: 1
  selector:
    matchLabels:
      app: palworld
  template:
    metadata:
      labels:
        app: palworld
    spec:
      terminationGracePeriodSeconds: 330
      securityContext:
        fsGroup: 1000
      containers:
        - name: palworld
          image: thijsvanloef/palworld-server-docker:v2.7.2
          lifecycle:
            preStop:
              exec:
                command:
                  - /bin/bash
                  - -c
                  - >-
                    rest-cli announce '{"message": "Server is restarting in 5 minutes. Please find a safe spot."}' &&
                    sleep 300 &&
                    rest-cli save &&
                    rest-cli shutdown '{"waittime": 10, "message": "Server is restarting now."}'
          resources:
            # Important workload (live game server) - no limits: an OOM-kill
            # or CPU throttle drops every connected player. Requests sized to
            # the observed steady-state baseline (~1.4-1.9Gi RSS, ~450m-860m
            # CPU with players on) rather than the old 4Gi/2-core placeholder.
            requests:
              memory: 2Gi
              cpu: 750m
          ports:
            - name: game
              containerPort: 8211
              protocol: UDP
            - name: query
              containerPort: 27015
              protocol: UDP
            - name: rest-api
              containerPort: 8212
              protocol: TCP
            - name: rcon
              containerPort: 25575
              protocol: TCP
          envFrom:
            - configMapRef:
                name: palworld-config
          env:
            - name: ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: palworld-secrets
                  key: ADMIN_PASSWORD
            - name: SERVER_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: palworld-secrets
                  key: SERVER_PASSWORD
            - name: DISCORD_WEBHOOK_URL
              valueFrom:
                secretKeyRef:
                  name: palworld-secrets
                  key: DISCORD_WEBHOOK_URL
          volumeMounts:
            - name: data
              mountPath: /palworld
        - name: playit
          image: ghcr.io/playit-cloud/playit-agent:1.0.10
          resources:
            requests:
              memory: 64Mi
              cpu: 10m
            limits:
              memory: 128Mi
          env:
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: palworld-secrets
                  key: PLAYIT_SECRET_KEY
        - name: exporter
          image: docker.io/banhcanh/palworld-exporter:v0.0.6
          ports:
            - name: metrics
              containerPort: 18212
              protocol: TCP
          env:
            # Scrapes the REST API (enabled by default in the server image,
            # REST_API_PORT=8212 in palworld-config), not RCON.
            - name: SCRAPE_URI
              value: 'http://127.0.0.1:8212/v1/api'
            - name: HTTP_USER
              value: 'admin'
            - name: HTTP_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: palworld-secrets
                  key: ADMIN_PASSWORD
            - name: SAVE_DIRECTORY
              value: '/palworld'
            - name: LISTEN_ADDRESS
              value: ':18212'
          resources:
            requests:
              memory: 64Mi
              cpu: 10m
            limits:
              memory: 128Mi
          volumeMounts:
            - name: data
              mountPath: /palworld
              readOnly: true
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: palworld-data
